Composition
 - Find a way to compose / decompose a data stream
 - The self-learning should be a function of frequency and anomalies

Procedural
 - Create classifiers for quick computation
Declarative
 - Create defined procedures for precise calculation

Vision
 - Find simple convolutions that are easy to compute for classifiers
    - Haar basis
 - Compare performance of general algorithm to existing algorithms
    - Edge detection, etc
 - Counting
    - Create images with random number of pixels
    - Train the procedural algorithm to identify the number of pixels
    - Train a declarative algorithm to count the number of pixels

Knowledge Graph vs Vector Database
 - Should be able to represent both ways
 - How do we translate between one and another
 - Kowledge graph as a graph of the internet
   - Graph of wikipedia
   - Graph of books?

Mathematics
 - Determine argument patterns
 - Determine new ways to find arguments from the perspective of knowledge graphs

Compression
 - Create different knowledge graphs
 - Represent data as a composition of different queries on an existing knowledge graph
 - Composition should provide exponential performance on compression
 - Compare to LZ76, Huffman encodings, etc


1  /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    2  echo >> /Users/b/.zprofile
    3  echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> /Users/b/.zprofile
    4  eval "$(/opt/homebrew/bin/brew shellenv)"
    5  brew install -cask zed
    6  brew install --cask zed
    7  brew install --cask docker
    8  python -version
    9  brew install python
   10  brew install python
   11  python -version
   12  python3 -version
   13  python3 --version
   14  brew install --cask docker
   15  brew uninstall docker
   16  brew install --cask docker
 269  nmap -sS 192.168.0.1/24
  270  sudo nmap -sS 192.168.0.1/24
  271  ping
  272  ifconfig | grep inet
  273  ifconfig | grep inet
  274  cd Code/moxie
  275  node server.js
  276  cd Code/moxie
  277  node server.js
  278  history
  279  history 0
  280  cd Code/mem
  281  cd Code/moxie
  282  npm install express socket.io
  283  npm install express socket.io
  284  npm install express socket.io
  285  node server.js
